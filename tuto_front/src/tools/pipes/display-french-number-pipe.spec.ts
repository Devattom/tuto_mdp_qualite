import { DisplayFrenchNumberPipe } from './display-french-number-pipe';

describe('DisplayFrenchNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayFrenchNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
